#!/bin/bash

docker run --init --name=portainer \
 -v /etc/hosts:/etc/hosts \
 -v /home/{{ admin_user }}/ops/portainer/data:/data \
 -v /var/run/docker.sock:/var/run/docker.sock \
 --env TZ=PRC \
 --network admin_network \
 -p '14003:9000' \
 --rm {{ harbor_ip }}/admin/portainer/portainer:latest